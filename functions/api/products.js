export async function onRequest(context) { const { env } = context; try { /* const { results } = await env.DB.prepare('SELECT * FROM products').all(); */ const results = [ { id: 1, name: 'Vogue Sphere', price: 249, description: 'Procedural artifact' }, { id: 2, name: 'Glass Torus', price: 399, description: 'Glassmorphism study' } ]; return new Response(JSON.stringify(results), { headers: { 'Content-Type': 'application/json' } }); } catch (e) { return new Response(e.message, { status: 500 }); } }